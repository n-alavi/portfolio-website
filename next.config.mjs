// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   // output: "export", // <=== enables static exports
//   reactStrictMode: true,
//   experimental: {
//     serverActions: true,
//   },
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export", // Uncomment this only if you want static exports
  reactStrictMode: true,
};

export default nextConfig;
